Create a function in js
1. Method 1
```
function RenderMenuItem({ something, onClick }) {
    return (
        <Card
            onClick={() => onClick(something.id)}>
        </Card>
    )
}
```

2. Method 2
```
const Menu = () => {
    return (
    )
}
```
